const aluno = {
    nome:"Laisa",
    sobrenome:"Duarte",
    nomeCompleto: function nomeCompleto(nome, sobrenome){
        return nome + " " + sobrenome;
    }
};

console.log(aluno.nome);
console.log(aluno.sobrenome);
console.log(aluno.nomeCompleto("gill", "gomes"));

nomeCompleto("out", "nome");

lista.length;

const lista = {
    length: 14
}

var texto = "José";

texto.toUpperCase();


String texto;
Integer numero;


const carlos = {
    nome: {
        sobrenome: "final"
    }
};

console.log(carlos.nome.sobrenome);